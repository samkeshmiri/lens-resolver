import { fetchGql } from '../utils/fetcher';
import { stringifyObjectValues } from '../utils/stringifyObjectValues';
export async function fetchQuery(query, variables) {
    const _variables = stringifyObjectValues(variables || {});
    let data = null;
    let error = null;
    const [response, _error] = await fetchGql(query, _variables);
    data = response;
    error = _error;
    return {
        data,
        error,
    };
}
//# sourceMappingURL=fetchQuery.js.map