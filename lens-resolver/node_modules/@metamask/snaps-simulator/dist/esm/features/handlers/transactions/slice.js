import { HandlerType } from "@metamask/snaps-utils";
import { createSelector } from "@reduxjs/toolkit";
import { createHandlerSlice } from "../slice";
export var INITIAL_STATE = {
    request: {},
    response: null,
    history: []
};
var slice = createHandlerSlice({
    name: HandlerType.OnTransaction,
    initialState: INITIAL_STATE
});
export var transactions = slice.reducer;
var _slice_actions = slice.actions;
export var setTransactionRequest = _slice_actions.setRequest, setTransactionRequestFromHistory = _slice_actions.setRequestFromHistory, setTransactionResponse = _slice_actions.setResponse, clearTransactionResponse = _slice_actions.clearResponse;
export var getTransactionRequest = createSelector(function(state) {
    return state[HandlerType.OnTransaction];
}, function(state) {
    return state.request;
});
