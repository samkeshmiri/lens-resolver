import { HandlerType } from "@metamask/snaps-utils";
import { createSelector } from "@reduxjs/toolkit";
import { createHandlerSlice } from "../slice";
export var INITIAL_STATE = {
    request: {
        origin: ""
    },
    response: null,
    history: []
};
var slice = createHandlerSlice({
    name: HandlerType.OnCronjob,
    initialState: INITIAL_STATE
});
export var cronjob = slice.reducer;
var _slice_actions = slice.actions;
export var setCronjobRequest = _slice_actions.setRequest, setCronjobRequestFromHistory = _slice_actions.setRequestFromHistory, setCronjobResponse = _slice_actions.setResponse, clearCronjobResponse = _slice_actions.clearResponse;
export var getCronjobRequest = createSelector(function(state) {
    return state[HandlerType.OnCronjob];
}, function(state) {
    return state.request;
});
