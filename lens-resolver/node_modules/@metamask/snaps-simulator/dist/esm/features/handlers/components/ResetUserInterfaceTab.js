import { useTabsContext } from "@chakra-ui/react";
import { useEffect } from "react";
import { useSelector } from "../../../hooks";
import { getUserInterface } from "../../simulation";
/**
 * Resets the tab to the first tab when the user interface is closed.
 *
 * @returns `null`.
 */ export var ResetUserInterfaceTab = function() {
    var userInterface = useSelector(getUserInterface);
    var tab = useTabsContext();
    useEffect(function() {
        if (!userInterface) {
            tab.setSelectedIndex(0);
        }
    }, [
        userInterface,
        tab
    ]);
    return null;
};
