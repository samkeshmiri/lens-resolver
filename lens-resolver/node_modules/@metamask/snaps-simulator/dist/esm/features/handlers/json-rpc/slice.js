import { HandlerType } from "@metamask/snaps-utils";
import { createSelector } from "@reduxjs/toolkit";
import { createHandlerSlice } from "../slice";
export var INITIAL_STATE = {
    request: {
        origin: ""
    },
    response: null,
    history: []
};
var slice = createHandlerSlice({
    name: HandlerType.OnRpcRequest,
    initialState: INITIAL_STATE
});
export var jsonRpc = slice.reducer;
var _slice_actions = slice.actions;
export var setJsonRpcRequest = _slice_actions.setRequest, setJsonRpcRequestFromHistory = _slice_actions.setRequestFromHistory, setJsonRpcResponse = _slice_actions.setResponse, clearJsonRpcResponse = _slice_actions.clearResponse;
export var getJsonRpcRequest = createSelector(function(state) {
    return state[HandlerType.OnRpcRequest];
}, function(state) {
    return state.request;
});
