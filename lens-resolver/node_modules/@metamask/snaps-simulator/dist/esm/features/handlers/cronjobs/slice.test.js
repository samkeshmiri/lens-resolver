function _define_property(obj, key, value) {
    if (key in obj) {
        Object.defineProperty(obj, key, {
            value: value,
            enumerable: true,
            configurable: true,
            writable: true
        });
    } else {
        obj[key] = value;
    }
    return obj;
}
function _object_spread(target) {
    for(var i = 1; i < arguments.length; i++){
        var source = arguments[i] != null ? arguments[i] : {};
        var ownKeys = Object.keys(source);
        if (typeof Object.getOwnPropertySymbols === "function") {
            ownKeys = ownKeys.concat(Object.getOwnPropertySymbols(source).filter(function(sym) {
                return Object.getOwnPropertyDescriptor(source, sym).enumerable;
            }));
        }
        ownKeys.forEach(function(key) {
            _define_property(target, key, source[key]);
        });
    }
    return target;
}
function ownKeys(object, enumerableOnly) {
    var keys = Object.keys(object);
    if (Object.getOwnPropertySymbols) {
        var symbols = Object.getOwnPropertySymbols(object);
        if (enumerableOnly) {
            symbols = symbols.filter(function(sym) {
                return Object.getOwnPropertyDescriptor(object, sym).enumerable;
            });
        }
        keys.push.apply(keys, symbols);
    }
    return keys;
}
function _object_spread_props(target, source) {
    source = source != null ? source : {};
    if (Object.getOwnPropertyDescriptors) {
        Object.defineProperties(target, Object.getOwnPropertyDescriptors(source));
    } else {
        ownKeys(Object(source)).forEach(function(key) {
            Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
        });
    }
    return target;
}
import { clearCronjobResponse, cronjob as reducer, INITIAL_STATE, setCronjobRequest, setCronjobRequestFromHistory, setCronjobResponse } from "./slice";
describe("cronjobs", function() {
    describe("setCronjobRequest", function() {
        it("sets the request", function() {
            var result = reducer(INITIAL_STATE, setCronjobRequest({
                origin: "foo"
            }));
            expect(result.request).toStrictEqual({
                origin: "foo"
            });
        });
        it("pushes the request to history", function() {
            var result = reducer(INITIAL_STATE, setCronjobRequest({
                origin: "foo"
            }));
            expect(result.history).toStrictEqual([
                {
                    date: expect.any(Date),
                    request: {
                        origin: "foo"
                    }
                }
            ]);
        });
    });
    describe("setCronjobRequestFromHistory", function() {
        it("sets the request", function() {
            var result = reducer(INITIAL_STATE, setCronjobRequestFromHistory({
                origin: "foo"
            }));
            expect(result.request).toStrictEqual({
                origin: "foo"
            });
        });
        it("does not push to history", function() {
            var result = reducer(INITIAL_STATE, setCronjobRequestFromHistory({
                origin: "foo"
            }));
            expect(result.history).toStrictEqual([]);
        });
    });
    describe("setCronjobResponse", function() {
        it("sets the response", function() {
            var result = reducer(INITIAL_STATE, setCronjobResponse("foo"));
            expect(result.response).toBe("foo");
        });
    });
    describe("clearResponse", function() {
        it("clears the response", function() {
            var result = reducer(_object_spread_props(_object_spread({}, INITIAL_STATE), {
                response: {
                    jsonrpc: "2.0",
                    id: null,
                    result: "foo"
                }
            }), clearCronjobResponse());
            expect(result.response).toBeNull();
        });
    });
});
