import { jsx as _jsx } from "react/jsx-runtime";
import { Tag } from "@chakra-ui/react";
import { useMatch } from "react-router-dom";
export var NavigationTag = function(param) {
    var children = param.children, path = param.path;
    var active = useMatch(path);
    var isActive = Boolean(active);
    return /*#__PURE__*/ _jsx(Tag, {
        borderRadius: "base",
        variant: "code",
        color: isActive ? "#0376C9" : "info.default",
        backgroundColor: isActive ? "rgba(3, 118, 201, 0.1)" : "info.muted",
        children: children
    });
};
