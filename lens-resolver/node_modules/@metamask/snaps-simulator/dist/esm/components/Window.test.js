import { jsx as _jsx } from "react/jsx-runtime";
import { render } from "../utils";
import { Window } from "./Window";
describe("Window", function() {
    it("renders", function() {
        expect(function() {
            return render(/*#__PURE__*/ _jsx(Window, {
                snapName: "foo-snap",
                snapId: "local:http://localhost:8000",
                children: "Foo"
            }));
        }).not.toThrow();
    });
});
