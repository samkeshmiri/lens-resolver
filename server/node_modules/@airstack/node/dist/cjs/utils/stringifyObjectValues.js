"use strict";
exports.__esModule = true;
exports.stringifyObjectValues = void 0;
function stringifyObjectValues(value) {
    var stringified = {};
    if (!value || typeof value !== "object")
        return value;
    for (var key in value) {
        if (Array.isArray(value[key])) {
            stringified[key] = value[key].map(function (item) {
                return stringifyObjectValues(item);
            });
        }
        else if (typeof value[key] === "object") {
            stringified[key] = JSON.stringify(value[key]);
        }
        else {
            stringified[key] = value[key];
        }
    }
    return stringified;
}
exports.stringifyObjectValues = stringifyObjectValues;
//# sourceMappingURL=stringifyObjectValues.js.map