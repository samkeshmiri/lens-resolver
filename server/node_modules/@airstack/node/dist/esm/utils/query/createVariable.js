import { Kind } from "graphql";
function getType(kind, value, required = false) {
    let type = {
        kind: "NamedType",
        name: {
            kind: "Name",
            value,
        },
    };
    if (required) {
        type = {
            kind: "NonNullType",
            type: Object.assign({}, type),
        };
    }
    if (kind === Kind.LIST) {
        type = {
            kind: "ListType",
            type: Object.assign({}, type),
        };
    }
    return type;
}
export function createVariable({ type, isRequired, name, uniqueName, valueKind, }) {
    return {
        kind: "VariableDefinition",
        variable: {
            kind: "Variable",
            name: {
                kind: "Name",
                value: uniqueName || name,
            },
        },
        type: getType(valueKind, type, isRequired),
        directives: [],
    };
}
//# sourceMappingURL=createVariable.js.map